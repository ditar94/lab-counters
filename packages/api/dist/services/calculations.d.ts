import type { CountRecordType } from '@lab-counters/shared';
/**
 * Calculate results based on counter type and data
 */
export declare function calculateResults(type: CountRecordType, data: unknown): unknown;
